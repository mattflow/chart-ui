{
  "name": "chart-ui",
  "version": "1.0.0",
  "description": "An free and simple to use interface for making good-looking charts and graphs.",
  "main": "server.js",
  "private": true,
  "scripts": {
    "postinstall": "cd client && yarn install",
    "dev": "concurrently \"nodemon --ignore client server\" \"cd client && yarn start\"",
    "deploy": "cd client && yarn deploy",
    "build": "cd client && yarn build",
    "start": "node server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "cors": "^2.8.5",
    "express": "^4.16.4"
  },
  "devDependencies": {
    "concurrently": "^4.1.0",
    "nodemon": "^1.18.7"
  }
}
